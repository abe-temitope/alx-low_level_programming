#include <main.h>

/**
 * main - Print alphabet in lowercase.
 *
 * Return: Always 0.
 */
void print_alphabet(void);
{
	char a = "a";

	while (a <= "z")
	{
		_putchar("%c", a);
		_putchar("\n");
		a++;
		return (0);
	}

int main(void)
{
	void print_alphabet(void)
	return (0);
}
