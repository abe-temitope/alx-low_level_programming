#include <stdio.h>

/**
 * main - Print alphabet in reverse
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alpha = 'z';

	while (alpha >= 'a')
	{
		putchar(alpha);
		z--;
	}
	putchar('\n');
	return (0);
}	
